{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "dependencies": {
    "shared": "workspace:*",
    "hono": "^4.9.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun --env-file=../../config/.env run --hot index.ts",
    "start": "bun run ./dist/index.js",
    "build": "bun build --target node index.ts --minify --outfile ./dist/index.mjs"
  }
}
