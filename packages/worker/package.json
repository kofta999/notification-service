{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "dependencies": {
    "shared": "workspace:*",
    "hono": "^4.9.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun --env-file=../../config/api.env --env-file=../../config/db.env run --hot index.ts",
    "start": "bun run ./dist/index.js",
    "build": "bun build --target bun index.ts --minify --outdir ./dist"
  }
}
